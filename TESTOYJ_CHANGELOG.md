# testoyj 更新日志

## 常驻致谢

1. [xylolit-mu/OnmyojiAutoScript（self 分支）](https://github.com/xylolit-mu/OnmyojiAutoScript/tree/self)：本分支任务实现与功能更新的主要参考来源；结界蹭卡、突破及百鬼棋局等功能均在其持续维护成果基础上结合本分支需求进行适配。
2. [AzurTian/OnmyojiAutoScript](https://github.com/AzurTian/OnmyojiAutoScript)：OAS 上游项目及基础框架来源，本分支持续跟进其通用功能、资源和稳定性更新。

本文件只记录 `XDeolx/OnmyojiAutoScript` 的 `testoyj` 分支改动，独立于上游 README 和发布说明。

## 2026-09-04

### 爬塔奖励页结算行为

- 新增默认关闭的“爬塔结算行为”：每次启动任务会从 11 个安全区域中重新抽取 A-E 模板，奖励页按 A 5%、B 5%、C 25%、D 20%、E 45% 选择实际点击区域。
- 新增可选的“模拟查看御魂详情”：触发周期在 20-40 场之间重新抽取，首次运行采用随机初始进度；点击详情后随机等待 1-1.5 秒，再从右下区域执行 3-4 次低偏移点击。
- 未触发查看详情时支持单独设置快速结算概率；特殊点击期间会检查奖励页是否已经退出，页面变化后立即停止，仍停留时回退到普通加权点击。
- 结算区域、点击坐标、详情进度、随机等待和快速点击次数均写入日志；与“随机休息疲劳”使用独立计数，疲劳结算等待仍保证每场只执行一次。

### 当期爬塔与每日登录礼包

- 重建当期爬塔页面路线：从活动主页进入爬塔选择页后，再分别进入门票、体力、100 体和首领分支；保留门票与体力之间的自动切换，补齐活动主页、选择页、奖励页、签到页和四类挑战入口的识别资源。
- 爬塔新增“随机休息疲劳”模式，与原有“随机休息”二选一。它会跨四类爬塔累计战斗次数，让战前随机延迟随进度逐步增大；达到周期后随机休息，休息时间不计入活动运行时长；奖励页退出前也会按单独的随机区间等待。
- 每日琐事的每日礼包兼容周年庆分阶段登录礼包。购买/领取后会先识别新礼包面板并安全返回礼包屋，同时保留旧奖励页处理，避免新面板遗留给后续商店流程。

### 爬塔困难模式与五倍券

- 参考 xylolit `self` 分支的当期爬塔更新，在保留本分支疲劳休息、随机休息和既有四类爬塔顺序的前提下，新增门票困难/简单模式与体力爬塔五倍券。
- 门票次数现在支持单个数字或“简单次数,困难次数”：旧的单数字配置保持为只运行简单模式；填写两个次数时会优先完成困难模式，再继续简单模式，两个模式独立计数。
- 困难门票沿用门票战斗配置，并复用百体爬塔的御魂切换配置，避免新增重复设置；选择失败或资源不足时只结束当前难度，不影响随后可执行的简单模式。
- 五倍券默认关闭，仅在体力爬塔生效。开启后按五倍状态同步开关，并同步按 30 体力、5 张体力门票和 1 张五倍券的实际消耗检查余量；五倍券耗尽或余量不足时自动关闭，回到普通挑战。
- 资源 OCR 增加按上一场实际消耗修正的保护，避免五倍挑战或困难门票一次扣除多张时被误判为识别异常。

### 庭院、幕间与新主题

- 新增“狐栖归处”庭院适配，覆盖庭院识别及探索、召唤、町中、宠物小屋等常用入口。
- 新增“拾光之窗”式神录/幕间适配，覆盖进入和退出式神录、御魂预设、分组、队伍切换、确认和替换御魂等路径。
- 新增“新语明宵”主题适配，覆盖底部展开栏以及式神录、阴阳寮、好友、每日、商店、组队、图鉴和旅行等入口；主题资源按用户选择映射，不影响默认主题和既有皮肤。

### 活动清理

- 武道大会活动已结束：从任务菜单移除对应入口，并移除已失效的活动资源。
- 已在旧配置中排程的武道大会任务会记录活动结束并安全完成本次调度，不会尝试访问被移除的资源，也不会影响其他任务。

## 2026-09-03

### 周计划每周刷新

- 新增默认关闭的“周刷新”规则：进入新的 ISO 周时，从不变的基础周计划独立生成本周实际时间，同一周内重启或重读不会重复随机，也不会累积上周偏移。
- 每个“任务 × 星期”独立按秒随机，默认在基础时间前后浮动 10–20 分钟；任务之间允许交换先后，不增加顺序约束。
- 支持排除固定任务、逐项设置最早与最晚时间边界，并默认冻结周三 04:00–10:00；所有新时间必须同时满足偏移范围、任务边界、当天范围和非冻结时段。
- 合法时间为空时保留基础时间并记录冲突，不会越过边界强行生成；但基础时间位于冻结段且无法移出时，会跳过当天该任务，冻结规则优先于固定任务规则；新增无落盘预览与立即刷新本周接口。

## 2026-09-02

### 鼬乐币商店购买

- 参考 xylolit 的每周购买更新，在现有“大富翁”任务中新增可选的鼬乐币勾玉礼盒购买，默认关闭。
- 开启后会进入鼬乐币商店，依次购买最多三个自动前移的勾玉礼盒；前排礼盒不再打开购买框时视为已购买并结束，鼬乐币不足时也会停止后续购买。
- 保留购买框与购买结果等待超时；未识别到明确状态时安全结束，避免连续误点。
- 每次购买前识别鼬乐币与礼盒价格，购买后核对实际扣除数量，异常时记录原因并停止后续购买。
- 保留本分支现有的 `RichMan` 配置与任务名，不引入 xylolit 的目录重命名及寮功勋商店寻路改动。

### 新旧登录界面兼容

- 保留旧版横排“进入游戏”OCR，并新增新版竖排按钮识别；旧规则未命中时自动尝试新版规则。
- 新版登录页实测仍能识别左下角年龄标志，实际失效点为竖排按钮只能稳定识别“进入”；新版规则按该文字匹配并点击按钮内部区域。
- 修改只影响进入游戏按钮，不改变角色选择、登录动画、弹窗处理和庭院登录成功确认逻辑。

## 2026-09-01

### 奖励页退出点击分布

- 参考 xylolit 的奖励页分区点击方案，结合本分支的行为分析结果，将每次退出奖励页的区域概率调整为左侧 5%、顶部 10%、右侧 45%、底部 40%。
- 通用战斗、探索、超鬼王、活动爬塔、武道大会及六道之门统一使用同一套分布；每次点击时独立抽取，不会在脚本启动后固定区域。
- 仅调整奖励页退出点击，胜负结算页和其他页面仍沿用原有随机逻辑；本分支已有的结算等待间隔保持不变。

### 个人突破进攻间隔

- 新增可选的“进攻前随机延迟”，默认关闭；开启后，选中个人突破对手并出现进攻按钮时在 1–3 秒内按小数随机等待后再点击。
- 等待期间持续确认挑战浮窗，进攻按钮消失时取消本次挑战，不会继续点击已失效目标。
- “退四次”每轮会在退出按钮可用后随机等待 1–2 秒再退出，退出完成且再次挑战按钮可用后再随机等待 1–2 秒进入下一轮；最终正常战斗不受影响。
- 统一任务级随机等待日志，执行时记录任务阶段和本次实际等待秒数，便于确认配置是否生效。

### 寮突破进攻间隔

- 修正寮突破“随机延迟”说明与实际代码不一致的问题；开启后，选择下一个目标前会随机等待 2–10 秒，并在日志中记录本次等待时间。
- 选中目标并弹出“组队 / 进攻”窗口后，默认随机等待 2–5 秒再点击进攻；等待期间目标失效或弹窗关闭会立即取消本次等待。

### 寮奖励收取

- 修复寮活动栏展开动画尚未结束时，只出现寮资金可能被单次识别漏掉的问题；展开后等待界面稳定，并在同一次寮主页停留中有限复查资金和体力。
- 收取任意一项奖励后会重新截图检查另一项，连续两轮没有奖励即结束，不会恢复为反复进寮检查。

## 2026-08-31

### Windows 免安装包

- 新增 `testoyj` Windows 免安装包发布链路，使用官方 easy-install 运行环境叠加本分支最新代码。
- 修复首版安装包缺少 `config/deploy.yaml`、导致 OASX 无法识别根目录的问题；发布前会完整检查 OASX 所需的部署配置、Python、Git 和安装器。
- 修复官方基础包关闭 OCR 服务、导致首次启动连接 `127.0.0.1:22268` 超时的问题；免安装包会强制启用 OCR 服务并在发布前校验。
- 安装包不包含用户配置、周计划、日志和截图，并保留浅层 `testoyj` Git 仓库，便于后续快速增量更新。
- Release 同时提供 SHA-256 校验文件；首次安装或从其他分支迁移时可直接解压到新目录使用。

### 百鬼棋局同步与模块化升级

- 以 xylolit `self` 分支的最新 Chess 实现为基础同步百鬼棋局，将单文件流程拆分为状态识别、经济系统、手牌操作、阵容策略和符咒策略等独立模块。
- 跟进 xylolit 8 月 31 日的荒川阵容调整，更新式神、站位、升星权重和限定御魂，并将符咒的第二羁绊收敛为唯一稳定候选。
- 新增荒川、海国、大江山、狐妖、冥府、流火和七角山阵容策略，以及完整的符咒品质、收益排序与刷新处理。
- 增加运行时间、匹配超时、保段位、鼬乐币满额退出等控制，并兼容升级前保存的阵容和执行次数配置。
- 保留结算页实际名次 OCR；同时保留本分支原有的“按剩余人数提前退出”选项，默认关闭，可与保段位逻辑独立使用。
- 将本分支的买卡、卖卡成功后点击记录清理适配到新版模块，避免正常连续运营被误判为重复点击卡死。
- 新版暂以“百鬼棋局（测试版）”显示；如实战运行不稳定，可切换到 `testoyj-chess-legacy` 分支使用升级前的旧版实现。

### 结界经验酒壶返回稳定性

- 修复收取经验酒壶后，过渡画面可能被全局页面识别误判为寮突破，导致返回寮结界时连续尝试错误路线的问题。
- 酒壶收取期间只在寮结界和经验酒壶两个相关页面中定向识别，不再受其他页面模板误命中影响。

### 百鬼棋局买卖稳定性

- 修复商店连续购买目标式神、手牌满后正常出售再继续购买时，被通用连续点击保护误判为卡死的问题。
- 只有在画面确认购买成功或手牌数量确认减少后才重置点击记录；未确认成功的买卖仍保留原有防卡死检测。

## 2026-08-30

### 结界卡选择准确性

- 修复四星卡可能被模板误判为五/六星，进而漏掉后续高收益卡的问题。
- 模板只负责定位候选卡，实际类型、星级和收益统一以详情页 OCR 结果为准。
- 普通模式和怠惰模式都会按真实奖励选卡，并在扫描后准确定位已记录的最优卡。

### 可选寮抽奖与随机等待

- 蹭卡收尾会默认顺手收取当前可见的寮资金和体力，该行为不受寮抽奖开关影响。
- 新增可选的“顺手进行寮抽奖”，默认关闭；开启后在蹭卡收尾时处理当前可用抽奖。
- 新增寮奖励随机等待，收取资金、体力及抽奖时可随机停顿 `2-4` 秒；抽奖异常或无次数时会自动退出。

### 结界蹭卡间隔修正

- “蹭卡最小运行间隔”调整为“寄养结束后随机休息基准”，下次运行时间为结界卡剩余时间加上本次随机休息。
- 每次实际休息使用配置值的 `0.6-0.9` 倍；例如卡剩 6 小时、基准设置 5 分钟时，下次会安排在约 6 小时 3 分至 6 小时 4 分 30 秒后。
- 日志会记录本次随机系数和实际间隔；设置为 `0` 时不抽取随机数，也不增加等待。

## 2026-08-29

### 周计划秒级调度

- 周计划时间同时兼容旧的 `HH:mm` 和新的 `HH:mm:ss` 格式。
- 新保存的周计划统一保留秒数，并在同步今日任务、计算下次运行和显示本周日期时按秒处理。
- 原有仅包含时、分的计划自动按 `:00` 秒兼容，无需手动迁移配置。

### 幕间主题适配

- 式神录幕间主题新增“花札幕台”，补齐式神录进出、御魂预设、御魂分组/队伍切换和御魂整理所需的识别资源。
- 修正“契光水境”主题的式神录识别区域，适配该主题下标识位置的偏移。

### 结界蹭卡与突破优化

- 蹭卡会消除同一位置的重复识别，完整扫描后按 OCR 奖励重新定位最优卡；达到斗鱼 `151` 或太鼓 `76` 时提前结束扫描。
- 怠惰模式优先选择五星以上目标，没有时回退到四星；同区没有合适卡时可继续检查跨区好友。
- 入场失败、坑位被占和寄养失败采用分级重试，连续失败后延迟 `10` 分钟，避免长时间卡在好友结界。
- 寮突破和个人突破增加操作间隔浮动，并改善挑战浮窗、战斗过渡和列表刷新处理。

### 连接稳定性与识别修正

- Minitouch 触控连接被重置或意外关闭时，首次只重建触控控制连接；连续失败后才升级为完整 ADB 重连，减少长时间运行后不必要的模拟器重连。
- MuMu Nemu IPC 兼容无标准输出句柄的桌面后台进程；无法捕获原生输出时仍继续执行 IPC 调用并进入原有错误恢复流程。
- 修正契灵盘子数量、式神类型和业原火票数的 OCR 区域，提高不同界面偏移下的识别成功率。

### 通用任务修复

- 连续点击“购买更多”达到限制后自动退出，避免购买流程无限循环。
- 修复每周杂事中破碎护符退出时的点击间隔，减少连续空白点击和误操作。
- FindJade 现在继承悬赏封印的战斗优先级等任务参数，仅覆盖找玉专用的协作设置。
- 修正年兽等待界面的反向识别区域，提高等待和关闭按钮的识别兼容性。

### 操作拟人化

- 全局点击坐标改为中心概率更高的分布，并为 Minitouch、MuMu Nemu IPC 和 Scrcpy 增加随机按压时长、轻微位移、触摸压力及滑动节奏变化；拟人化操作始终生效，不设置开关。
- 结界蹭卡新增可配置的寄养结束后休息间隔；默认值为 `0`，保持原调度行为。

## 2026-08-28

### ADB 稳定性

- 修复 MuMu 的 ATX/uiautomator2 短暂断连后，ADB 返回空错误信息会被误判为不可恢复故障的问题。
- 空 `AdbError` 现在会重连当前模拟器实例并进入既有重试流程，不再第一次失败就直接要求人工接管；带有明确未知原因的 ADB 错误仍保持人工检查。

### 统计准确性

- 修复任务结束后的挂机等待被错误计入上一任务运行时长的问题；统计解析器现在以 `Scheduler: End task` 的时间作为任务结束时间。
- 历史原始日志会按新逻辑重新解析，无需修改或删除旧日志。

### 周计划休息日

- 周计划中曾编排、但当天没有安排的任务会在每日同步时自动停用，并在下一个有安排的日期重新启用和校时。
- 休息日停用只作用于周计划管理的任务，不影响从未加入周计划的主调度器任务；例如道馆只安排周一至周四后，周五至周日不会继续执行或按失败间隔重试。

## 2026-08-27

### 启动稳定性

- 修复 OASX 首次拉起 MuMu 12 时，ADB 已上线但 `MuMuPlayer` 渲染子窗口尚未就绪，导致 OAS 读取空句柄树并报 `IndexError: tuple index out of range` 的问题。
- 句柄树初始化最多等待并重建 10 次；仍未就绪时交由设备初始化流程重试，不再直接退出脚本进程。
- 截图句柄改为实际使用时再解析，并对 MuMu 空子窗口增加明确保护。

### 龟缩模式调度

- 修复切换龟缩模式或调整保留任务时重置调度时间的问题；模式切换现在只修改任务启用状态，并保留任务自身的成功间隔与失败重试时间。
- 周计划新增龟缩模式和保留任务列表；启用后仅保留所选任务，并停用主调度器中的其他任务。
- 关闭龟缩模式或停用周计划时，自动恢复周计划中原有任务的启用状态，并保留主调度器中的现有执行时间。
- 龟缩模式只在启用、手动同步和每日首次同步时调整任务，不会反复覆盖任务自身的成功间隔或失败重试。
- 接口兼容旧版 OASX：未提交龟缩字段时保持已有设置，并拒绝启用空保留列表。

### 自由循环任务

- 周计划新增自由循环任务集合，默认包含结界挂卡和结界蹭卡。
- 每日零点自动同步时，当天计划中的自由循环任务只确保启用并保留主调度器现有执行时间；其他任务仍按当天计划时刻同步。
- 手动执行“立即同步今日计划”仍会强制按周计划时刻校时，便于需要时主动重置任务时间。

### 结界与重启优化

- 结界蹭卡增加好友分组回退、四星以上筛选、失败重试和怠惰模式。
- 蹭卡退出时只在必经的寮主页单次处理可见奖励，不再反复进寮检查；寄养收获、体力盒和经验壶仍由各自开关控制。
- 寮突破连续三次未进入战斗时，按目标已被其他成员击破处理，避免重复点击。
- 重启流程可以识别并取消返回百鬼棋局，减少登录和回庭院时的流程中断。

## 2026-08-26

### 周计划后端

- 新增按配置保存的周计划，可为周一至周日分别编排任务和运行时间。
- 新增周计划查询、保存和“立即同步今日计划”接口，供 OASX 周计划页面使用。
- 返回 OAS 服务端当前时间、本周周一、计划日期、今日同步时间，以及计划内和计划外任务状态。
- 支持“补跑今日已过计划”：开启后，当天已经错过的计划会进入主调度器；关闭后会跳过，不覆盖这些任务当前的调度时间。
- 周计划启用后，每个自然日只自动同步一次。OAS 持续运行时会在次日 00:00 唤醒调度器；OAS 未运行时，会在下次启动后同步当天计划。
- 每日同步采用合并逻辑：只启用并更新时间表中当天安排的任务，不清空主调度器，也不修改当天未安排的任务。
- 同步完成后，任务成功间隔、失败重试和其他重新调度仍由 OAS 主调度器负责，周计划不会在当天反复覆盖。
- 周计划数据保存在 `config/weekly_schedule/`，该目录中的个人计划文件不会提交到 Git。

### 弈棋自动化

- 新增弈棋任务的自动执行流程。
- 补充 GameUi 页面导航，并放宽相关页面切换处理，提高弈棋流程兼容性。

### 兼容说明

- 周计划界面需要配套使用 `XDeolx/OASX` 的 `testoyj` 分支。
- 上游 OASX `v0.3.12` 不包含周计划编辑界面。
- `config/*.json` 中的账号配置属于本机数据，已由 `.gitignore` 排除，不会随分支或构建产物发布。

## 分支说明

- `testoyj` 包含独立开发的周计划、调度保护和界面配套能力，也参考了上游及社区分支中的部分稳定性修复和任务实现；以上日志按本分支最终实际行为记录。
- 主要参考来源、顺序与链接见文首“常驻致谢”。
- 分支仓库：<https://github.com/XDeolx/OnmyojiAutoScript/tree/testoyj>
- 配套 OASX 更新日志：<https://github.com/XDeolx/OASX/blob/testoyj/TESTOYJ_CHANGELOG.md>
